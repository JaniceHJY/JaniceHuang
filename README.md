# Janice Huang — Resume

A responsive, accessible resume website for Janice Huang, FRM. Built as a lightweight static site for GitHub Pages.

## View locally

Open `index.html` in a browser, or serve this directory with any static web server.

## Deployment

The included GitHub Pages workflow publishes the site whenever changes are pushed to `main`.
